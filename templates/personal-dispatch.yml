name: relay-dispatch

on:
  workflow_dispatch:
    inputs:
      relay_lane:
        required: true
        type: choice
        options:
          - hosted
          - fallback
          - device
          - burst
      relay_target:
        required: false
        type: string
        default: ""
      relay_reason:
        required: false
        type: string
        default: ""

permissions:
  contents: read

concurrency:
  group: relay-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  hosted:
    if: inputs.relay_lane == 'hosted'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - run: ./scripts/ci

  fallback:
    if: inputs.relay_lane == 'fallback'
    runs-on: [self-hosted, Linux, ARM64, fallback]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - run: ./scripts/ci

  device:
    if: inputs.relay_lane == 'device' && (inputs.relay_target == 'papyrus' || inputs.relay_target == 'rover' || inputs.relay_target == 'colak')
    runs-on: [self-hosted, Linux, ARM64, device, "${{ inputs.relay_target }}"]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - run: ./scripts/ci

  burst:
    if: inputs.relay_lane == 'burst'
    runs-on: [self-hosted, Linux, ARM64, burst]
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - run: ./scripts/ci
